# PrettyChannelNames

`PrettyChannelNames` is an Equicord/Vencord-style userplugin that makes visible text channel names easier to read by replacing hyphens with spaces and applying capitalization.

## Install

1. Clone this repo into `Equicord/src/userplugins`
2. Run `pnpm build` from the Equicord root
3. Restart Discord
4. Enable `PrettyChannelNames`